# 🚀 快速启动指南

## ✅ 已完成的改造

### 核心改动
1. ✅ **首页改为列表页** - 打开小程序直接看到挪车码列表
2. ✅ **新增独立创建页** - 点击"新增挪车码"才进入表单页面
3. ✅ **优化用户体验** - 符合常规应用的使用习惯
4. ✅ **后端支持完整** - 列表查询、创建、删除功能齐全

---

## 📱 应用结构（新版）

```
安心挪车码小程序
├── 📋 首页 (pages/index)      ← 应用入口，显示列表
│   ├── 未登录：欢迎界面 + 登录按钮
│   ├── 已登录：
│   │   ├── [+ 新增挪车码] 按钮
│   │   ├── 空状态提示（无数据时）
│   │   └── 车辆列表卡片
│   │       ├── [预览] 按钮
│   │       └── [删除] 按钮
│
├── ➕ 创建页 (pages/create)   ← 点击"新增"进入
│   ├── 返回按钮
│   ├── 表单字段
│   │   ├── 车主称呼 *
│   │   ├── 车牌号
│   │   ├── 手机号 *
│   │   └── 安慰语
│   └── [生成二维码] 按钮
│
└── 📞 呼叫页 (pages/call)     ← 扫码或预览进入
    ├── 车辆信息
    ├── 安慰语
    ├── 脱敏电话
    └── [呼叫车主] 按钮
```

---

## 🔧 启动步骤

### 1️⃣ 启动后端服务

```bash
cd D:\workspace\car-phone
mvn spring-boot:run
```

后端将在 `http://localhost:8081` 启动

### 2️⃣ 打开微信小程序

1. 打开**微信开发者工具**
2. 导入项目：选择 `D:\workspace\car-phone\miniprogram` 目录
3. 等待编译完成

### 3️⃣ 配置后端地址

确认 `miniprogram/app.js` 中的配置：

```javascript
App({
  globalData: {
    apiBase: "http://127.0.0.1:8081",  // 确保地址正确
    token: ""
  },
  // ...
});
```

**注意**：
- 模拟器测试：使用 `127.0.0.1` 或 `localhost`
- 真机测试：改为电脑的局域网 IP（如 `192.168.1.100`）

### 4️⃣ 开启"不校验合法域名"

在微信开发者工具中：
1. 点击右上角 **详情**
2. 找到 **本地设置**
3. 勾选 **不校验合法域名、web-view（业务域名）、TLS 版本以及 HTTPS 证书**

---

## 🎯 测试流程

### 测试1：首次使用

1. **打开小程序** → 看到欢迎界面
   ```
   ┌─────────────────────┐
   │  欢迎使用安心挪车码  │
   │                     │
   │  [微信登录]         │
   └─────────────────────┘
   ```

2. **点击登录** → 授权成功

3. **看到空状态** → 提示创建第一个挪车码
   ```
   ┌─────────────────────┐
   │  [+ 新增挪车码]     │
   │                     │
   │       🚗            │
   │    暂无挪车码       │
   └─────────────────────┘
   ```

4. **点击"新增挪车码"** → 进入创建页

5. **填写表单**：
   - 车主称呼：王先生
   - 车牌号：沪A12345
   - 手机号：13800138000
   - 安慰语：（默认或自定义）

6. **点击"生成二维码"** → 提示成功

7. **自动返回列表** → 看到新创建的挪车码
   ```
   ┌─────────────────────┐
   │  [+ 新增挪车码]     │
   │                     │
   │  ┌───────────────┐  │
   │  │ 沪A12345      │  │
   │  │ 王先生        │  │
   │  │ 138****8000   │  │
   │  │  [预览][删除] │  │
   │  └───────────────┘  │
   └─────────────────────┘
   ```

### 测试2：继续创建

1. 再次点击 **"+ 新增挪车码"**
2. 填写另一辆车的信息
3. 提交后返回列表
4. 列表中显示两条记录

### 测试3：预览功能

1. 点击任意记录的 **[预览]** 按钮
2. 进入呼叫页，看到完整信息
3. 可以点击 **[呼叫车主]** 测试拨号

### 测试4：删除功能

1. 点击任意记录的 **[删除]** 按钮
2. 确认对话框中选择"确定"
3. 列表自动刷新，该记录消失

---

## 🎨 界面预览

### 首页 - 现代化设计
- 紫色渐变标题栏
- 醒目的"新增"按钮
- 卡片式列表展示
- 优雅的阴影效果

### 创建页 - 简洁表单
- 顶部返回按钮
- 清晰的字段布局
- 必填项红色标记
- 温馨提示信息框

### 呼叫页 - 友好提示
- 大号车牌显示
- 温馨的安慰语
- 脱敏电话号码
- 友好的使用提示

---

## 📊 功能清单

| 功能 | 状态 | 说明 |
|------|------|------|
| 微信登录 | ✅ | 获取用户身份 |
| 查看列表 | ✅ | 显示所有挪车码 |
| 新增挪车码 | ✅ | 独立创建页面 |
| 预览效果 | ✅ | 模拟扫码体验 |
| 删除记录 | ✅ | 管理挪车码 |
| 扫码呼叫 | ✅ | 真实拨号功能 |
| 空状态提示 | ✅ | 无数据时友好提示 |
| 加载状态 | ✅ | 网络请求时显示 |
| 错误处理 | ✅ | 完善的异常提示 |

---

## 🔍 常见问题

### Q1: 登录后看不到列表？
**A**: 检查后端是否正常运行，查看控制台是否有错误信息

### Q2: 点击"新增"没反应？
**A**: 确认是否已登录，未登录时会提示"请先登录"

### Q3: 创建成功后列表没刷新？
**A**: 检查控制台是否有错误，可能是网络请求失败

### Q4: 真机测试无法连接？
**A**: 
1. 将 `apiBase` 改为电脑IP（非127.0.0.1）
2. 确保手机和电脑在同一WiFi
3. 检查防火墙是否允许8081端口

### Q5: 二维码扫描后打不开？
**A**: 
1. 确保后端 `APP_PUBLIC_BASE_URL` 配置正确
2. 本地测试需要后端可被外网访问

---

## 📝 开发建议

### 调试技巧
1. **查看日志**：微信开发者工具的控制台
2. **网络监控**：查看 Network 面板的请求
3. **数据检查**：使用 App Data 面板查看实时数据

### 优化方向
1. 添加二维码图片保存功能
2. 支持编辑已有挪车码
3. 添加搜索/筛选功能
4. 支持分享挪车码给他人

---

## ✨ 下一步

现在你可以：
1. ✅ 启动后端服务
2. ✅ 打开微信开发者工具
3. ✅ 按照测试流程进行完整测试
4. ✅ 根据需要进行功能扩展

**祝你使用愉快！** 🎉🚗
