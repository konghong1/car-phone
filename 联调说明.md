# 安心挪车码 - 前后端联调指南

## 📱 应用结构

### 页面说明
1. **首页 (pages/index)** - 挪车码列表页（应用入口）
   - 显示所有已创建的挪车码
   - 提供“新增挪车码”按钮
   - 支持预览和删除操作

2. **创建页 (pages/create)** - 创建挪车码表单页
   - 填写车主信息
   - 生成新的挪车码

3. **呼叫页 (pages/call)** - 扫码后的呼叫页面
   - 显示车辆信息和安慰语
   - 提供呼叫按钮

## ✅ 已完成的改动

### 后端改动 (Java Spring Boot)

1. **VehicleController.java**
   - ✅ 新增 `DELETE /api/vehicles/{id}` 接口，支持删除挪车码记录
   - ✅ 添加权限验证，确保只能删除自己的记录

2. **VehicleService.java**
   - ✅ 新增 `delete()` 方法，处理删除业务逻辑
   - ✅ 添加 `ForbiddenException` 异常类，处理无权访问情况

3. **InMemoryStore.java**
   - ✅ 新增 `deleteVehicle()` 方法，支持从内存中删除车辆记录

### 前端改动 (微信小程序)

1. **pages/index/index.js** - 首页逻辑
   - ✅ 简化为纯列表页，移除表单相关代码
   - ✅ 添加 `goToCreate()` 方法，跳转到创建页面
   - ✅ 保留登录、加载列表、预览、删除功能
   - ✅ 优化登录状态管理

2. **pages/index/index.wxml** - 首页界面
   - ✅ 重新设计为应用主页风格
   - ✅ 顶部渐变标题栏
   - ✅ 未登录时显示欢迎界面
   - ✅ 已登录后显示“新增挪车码”按钮
   - ✅ 空状态提示（无挪车码时）
   - ✅ 车辆列表卡片展示

3. **pages/index/index.wxss** - 首页样式
   - ✅ 现代化渐变设计
   - ✅ 卡片式列表布局
   - ✅ 响应式交互效果
   - ✅ 空状态美化

4. **pages/create/index.js** ⭐ 新增
   - ✅ 独立的创建页面逻辑
   - ✅ 表单验证和提交
   - ✅ 创建成功后返回列表并刷新
   - ✅ 自动返回上一页

5. **pages/create/index.wxml** ⭐ 新增
   - ✅ 完整的表单界面
   - ✅ 返回按钮
   - ✅ 温馨提示信息

6. **pages/create/index.wxss** ⭐ 新增
   - ✅ 优雅的表单样式
   - ✅ 渐变色按钮
   - ✅ 输入框焦点效果

4. **pages/call/index.wxml**
   - ✅ 添加加载状态提示
   - ✅ 优化默认安慰语显示
   - ✅ 添加友好提示信息

5. **pages/call/index.wxss**
   - ✅ 添加加载文本样式 `.loading-text`
   - ✅ 添加提示文本样式 `.tip`

6. **pages/call/index.js**
   - ✅ 优化 `callOwner()` 方法，添加错误处理

---

## 🚀 联调步骤

### 1. 启动后端服务

```bash
# 在项目根目录执行
mvn spring-boot:run
```

或者在 IDE 中运行 `CarPhoneApplication.java`

后端将在 `http://localhost:8081` 启动

### 2. 配置小程序

打开微信开发者工具，确保 `miniprogram/app.js` 中的 `apiBase` 配置正确：

```javascript
App({
  globalData: {
    apiBase: "http://127.0.0.1:8081",  // 确保与后端地址一致
    token: ""
  },
  // ...
});
```

**注意**：如果在真机测试，需要将 `127.0.0.1` 改为你的电脑 IP 地址（如 `192.168.x.x`）

### 3. 测试流程

#### 场景一：首次使用 - 登录并创建挪车码

1. **打开小程序**，进入首页（列表页）
2. 显示欢迎界面，点击“微信登录”
3. 登录成功后，自动跳转到已登录状态
4. 如果暂无挪车码，显示空状态提示
5. 点击 **“新增挪车码”** 按钮
6. 进入创建页面，填写信息：
   - 车主称呼（必填）：例如“王先生”
   - 车牌号（选填）：例如“沪A12345”
   - 手机号（必填）：例如“13800138000”
   - 安慰语（选填）：可自定义或使用默认值
7. 点击“生成二维码”按钮
8. 系统提示“生成成功”，自动返回列表页
9. 列表页自动刷新，显示新创建的挪车码

#### 场景二：查看挪车码列表

1. 登录成功后，首页自动显示所有挪车码
2. 每条记录以卡片形式展示：
   - **车牌号**（粗体大字）
   - 车主称呼
   - 脱敏手机号（如：138****8000）
3. 每张卡片有两个操作按钮：
   - **预览**：跳转到扫码页面，模拟他人扫码效果
   - **删除**：确认后删除该记录
4. 点击顶部 **“新增挪车码”** 按钮可继续创建

#### 场景三：扫码呼叫（模拟）

1. 在列表中点击任意记录的"预览"按钮
2. 进入扫码页面，显示：
   - 车牌号
   - 安慰语
   - 脱敏手机号
   - "呼叫车主"按钮
3. 点击"呼叫车主"按钮
4. 系统调用微信电话功能，拨打车主手机号

#### 场景四：删除挪车码

1. 在列表中找到要删除的记录
2. 点击"删除"按钮
3. 确认对话框中选择"确定"
4. 系统提示"删除成功"，列表自动刷新

---

## 📋 API 接口说明

### 1. 微信登录
- **接口**: `POST /api/auth/wechat-login`
- **请求体**: `{ "code": "微信登录code", "nickname": "" }`
- **响应**: `{ "token": "...", "ownerId": "...", "openid": "..." }`

### 2. 创建挪车码
- **接口**: `POST /api/vehicles`
- **请求头**: `Authorization: Bearer {token}`
- **请求体**: 
```json
{
  "ownerName": "王先生",
  "plateNo": "沪A12345",
  "phone": "13800138000",
  "comfortMessage": "您好，给您添麻烦了..."
}
```
- **响应**: 完整的车辆信息对象

### 3. 查询我的挪车码列表
- **接口**: `GET /api/vehicles`
- **请求头**: `Authorization: Bearer {token}`
- **响应**: 车辆信息数组

### 4. 获取二维码图片
- **接口**: `GET /api/vehicles/{id}/qrcode`
- **响应**: PNG 格式的图片二进制数据

### 5. 获取公开车辆信息（扫码页使用）
- **接口**: `GET /api/public/vehicles/{id}`
- **响应**: 
```json
{
  "id": "...",
  "ownerName": "王先生",
  "plateNo": "沪A12345",
  "maskedPhone": "138****8000",
  "phone": "13800138000",
  "comfortMessage": "您好，给您添麻烦了..."
}
```

### 6. 删除挪车码 ⭐ 新增
- **接口**: `DELETE /api/vehicles/{id}`
- **请求头**: `Authorization: Bearer {token}`
- **响应**: 204 No Content（无内容）

---

## 🎯 核心功能验证清单

- [ ] 微信登录成功，获取 token
- [ ] 创建挪车码成功，返回完整信息
- [ ] 创建成功后，表单自动清空
- [ ] 车辆列表正确显示所有记录
- [ ] 列表按创建时间倒序排列
- [ ] 点击"预览"能正确跳转到扫码页
- [ ] 扫码页显示正确的安慰语和脱敏电话
- [ ] 点击"呼叫车主"能调起拨号功能
- [ ] 点击"删除"能正确删除记录
- [ ] 删除后列表自动刷新
- [ ] 二维码图片能正常显示
- [ ] 多次创建会生成多条独立记录

---

## 🔧 常见问题

### 1. 小程序请求失败
- 检查后端是否正常运行
- 检查 `apiBase` 地址是否正确
- 检查微信开发者工具的"不校验合法域名"选项是否开启

### 2. 真机测试无法连接
- 确保手机和电脑在同一局域网
- 将 `apiBase` 改为电脑的局域网 IP（如 `192.168.1.100`）
- 检查防火墙是否允许 8081 端口

### 3. 登录失败
- 开发环境下，后端会使用演示模式（demo-openid）
- 如需真实微信登录，需在 `application.yml` 中配置真实的 AppID 和 AppSecret

### 4. 二维码无法扫描
- 确保后端 `APP_PUBLIC_BASE_URL` 配置正确
- 如果是本地测试，确保二维码中的 URL 能被外部访问

---

## 📱 界面预览

### 首页 - 未登录状态
```
┌─────────────────────────┐
│   安心挪车码             │
│   管理您的挪车二维码     │
├─────────────────────────┤
│                         │
│   欢迎使用安心挪车码    │
│                         │
│   登录后可以创建和管理  │
│   您的挪车二维码        │
│                         │
│   [微信登录]            │
│                         │
└─────────────────────────┘
```

### 首页 - 已登录但无数据
```
┌─────────────────────────┐
│   安心挪车码             │
│   管理您的挪车二维码     │
├─────────────────────────┤
│                         │
│   [+ 新增挪车码]        │
│                         │
│         🚗              │
│      暂无挪车码         │
│   点击上方按钮创建您的  │
│   第一个挪车码          │
│                         │
└─────────────────────────┘
```

### 首页 - 有数据列表
```
┌─────────────────────────┐
│   安心挪车码             │
│   管理您的挪车二维码     │
├─────────────────────────┤
│                         │
│   [+ 新增挪车码]        │
│                         │
│ ┌─────────────────────┐ │
│ │ 沪A12345            │ │
│ │ 王先生              │ │
│ │ 138****8000         │ │
│ │        [预览][删除] │ │
│ └─────────────────────┘ │
│                         │
│ ┌─────────────────────┐ │
│ │ 沪B67890            │ │
│ │ 李女士              │ │
│ │ 139****9000         │ │
│ │        [预览][删除] │ │
│ └─────────────────────┘ │
└─────────────────────────┘
```

### 创建页 - 表单页面
```
┌─────────────────────────┐
│ ← 返回    创建挪车码     │
├─────────────────────────┤
│                         │
│ 车主称呼 *              │
│ [__________________]    │
│                         │
│ 车牌号                  │
│ [__________________]    │
│                         │
│ 手机号 *                │
│ [__________________]    │
│                         │
│ 安慰语                  │
│ [__________________]    │
│ [__________________]    │
│                         │
│ 💡 温馨提示：每次创建   │
│    都会生成一条新的挪车 │
│    码记录...            │
│                         │
│   [生成二维码]          │
│                         │
└─────────────────────────┘
```

### 扫码页（呼叫端）
```
┌─────────────────────────┐
│   联系车主挪车           │
│                         │
│     沪A12345            │
│                         │
│  您好，给您添麻烦了。    │
│  车主已开启挪车电话，    │
│  请点击下方按钮联系车主。 │
│                         │
│  车主电话：138****8000   │
│                         │
│  [呼叫车主]              │
│                         │
│  提示：请确认需要挪车    │
│  后再拨打，感谢理解。    │
└─────────────────────────┘
```

---

## ✨ 功能亮点

1. **应用化设计**：首页是标准的挪车码管理应用界面
2. **独立创建页**：点击“新增”才进入表单页面，流程清晰
3. **列表管理**：卡片式展示所有挪车码，一目了然
4. **多次创建**：每次创建生成独立记录，支持多辆车
5. **即时反馈**：创建成功后自动返回列表并刷新
6. **灵活操作**：支持预览和删除任意记录
7. **友好体验**：完善的空状态、加载状态和错误提示
8. **权限控制**：只能查看和管理自己的挪车码

---

## 🎉 联调完成！

现在你可以：
1. 启动后端服务
2. 在微信开发者工具中打开小程序
3. 按照测试流程进行完整的功能测试
4. 如有问题，查看控制台日志和后端日志进行排查

祝联调顺利！🚀
