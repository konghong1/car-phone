# 车主档案多账号架构设计

## 🎯 核心需求

**一个微信用户可以创建多个车主档案（手机号+昵称组合）**，然后选择其中一个档案去制作挪车贴图。

---

## 📊 数据模型

### 1. OwnerProfile（车主档案）

```java
public record OwnerProfile(
    String id,           // 档案ID
    String openid,       // 微信openid（关联微信用户）
    String nickname,     // 昵称
    String phone,        // 手机号
    Instant createdAt,
    Instant updatedAt
)
```

**特点：**
- 一个openid可以有多个OwnerProfile
- 每个Profile代表一个"身份"（如：本人、家人、朋友等）
- 包含独立的昵称和手机号

### 2. VehicleCard（挪车卡片）

```java
public record VehicleCard(
    String id,
    String ownerId,      // 关联的车主档案ID（不是openid）
    String vehicleId,    // 车辆ID
    String ownerName,    // 车主姓名（从profile复制）
    String plateNo,      // 车牌号
    String phone,        // 电话（从profile复制）
    String comfortMessage,
    String cardStyle,
    String backgroundColor,
    String themeColor,
    Boolean showPlateBadge,
    String customText,
    Instant createdAt,
    Instant updatedAt
)
```

**特点：**
- 通过ownerId关联到具体的OwnerProfile
- 创建时从Profile复制昵称和电话（快照）
- 同一辆车可以用不同的Profile创建多个卡片

---

## 🏗️ 业务架构

### 层级关系

```
微信用户（OpenID: oXXXXX）
  ├─ 车主档案1：王先生 / 13800138000
  │    ├─ 京A88888 - 现代简约风格
  │    └─ 沪B66666 - 经典商务风格
  ├─ 车主档案2：王经理 / 13900139000
  │    └─ 京A88888 - 极简风格（工作用）
  └─ 车主档案3：小王 / 13700137000
       └─ 粤C99999 - 可爱卡通风格（给家人用）
```

### 使用场景

#### 场景1：个人多号码
```
用户有多个手机号：
- 档案1：主号码 138****8000（日常使用）
- 档案2：备用号码 139****9000（工作使用）

可以为同一辆车创建两个挪车码，分别显示不同号码
```

#### 场景2：家庭共用
```
家庭成员共用一个微信：
- 档案1：丈夫 - 张先生 / 138****1111
- 档案2：妻子 - 李女士 / 139****2222

各自为自己的车创建挪车码
```

#### 场景3：多角色
```
一个人有多个身份：
- 档案1：本人（私家车）
- 档案2：司机（工作用车）
- 档案3：代驾（临时使用）
```

---

## 🔌 API接口

### 车主档案管理

#### 1. 获取所有档案
```http
GET /api/profiles
Authorization: Bearer {token}

Response:
[
  {
    "id": "profile_001",
    "openid": "oXXXXX",
    "nickname": "王先生",
    "phone": "13800138000",
    "createdAt": "2024-01-01T00:00:00Z",
    "updatedAt": "2024-01-01T00:00:00Z"
  },
  {
    "id": "profile_002",
    "openid": "oXXXXX",
    "nickname": "王经理",
    "phone": "13900139000",
    "createdAt": "2024-01-02T00:00:00Z",
    "updatedAt": "2024-01-02T00:00:00Z"
  }
]
```

#### 2. 创建新档案
```http
POST /api/profiles
Authorization: Bearer {token}
Content-Type: application/json

{
  "nickname": "小王",
  "phone": "13700137000"
}

Response:
{
  "id": "profile_003",
  "openid": "oXXXXX",
  "nickname": "小王",
  "phone": "13700137000",
  "createdAt": "2024-01-03T00:00:00Z",
  "updatedAt": "2024-01-03T00:00:00Z"
}
```

#### 3. 更新档案
```http
PUT /api/profiles/{id}
Authorization: Bearer {token}
Content-Type: application/json

{
  "nickname": "王大伟",
  "phone": "13800138001"
}
```

#### 4. 删除档案
```http
DELETE /api/profiles/{id}
Authorization: Bearer {token}

注意：删除档案会同时删除该档案创建的所有挪车卡片
```

### 挪车卡片管理

#### 1. 创建卡片（选择档案）
```http
POST /api/vehicles
Authorization: Bearer {token}
Content-Type: application/json

{
  "ownerId": "profile_001",  // 选择使用哪个档案
  "plateNo": "京A88888",
  "comfortMessage": "您好，麻烦挪一下车",
  "cardStyle": "modern",
  "backgroundColor": "#ffffff",
  "themeColor": "#667eea",
  "showPlateBadge": true,
  "customText": ""
}

Response:
{
  "id": "card_001",
  "vehicleId": "vehicle_001",
  "ownerName": "王先生",      // 从profile_001复制
  "plateNo": "京A88888",
  "phone": "13800138000",     // 从profile_001复制
  ...
}
```

#### 2. 获取我的所有卡片
```http
GET /api/vehicles
Authorization: Bearer {token}

返回当前用户所有档案创建的卡片列表
```

---

## 💻 前端实现要点

### 1. 档案选择流程

```javascript
// 步骤1：加载所有档案
loadProfiles() {
  wx.request({
    url: `${apiBase}/api/profiles`,
    header: { Authorization: `Bearer ${token}` },
    success: ({ data }) => {
      this.setData({ profiles: data });
    }
  });
}

// 步骤2：用户选择档案
selectProfile(event) {
  const profileId = event.currentTarget.dataset.profileId;
  this.setData({ selectedProfileId: profileId });
}

// 步骤3：创建卡片时传入ownerId
createCard() {
  wx.request({
    url: `${apiBase}/api/vehicles`,
    method: 'POST',
    data: {
      ownerId: this.data.selectedProfileId,  // 关键！
      plateNo: this.data.plateNo,
      ...
    }
  });
}
```

### 2. UI设计建议

#### 方案A：先选档案，再创建卡片
```
┌─────────────────────────────┐
│  选择车主档案                │
├─────────────────────────────┤
│  ○ 王先生  138****8000      │
│  ○ 王经理  139****9000      │
│  ○ 小王    137****7000      │
│                             │
│  [+ 添加新档案]              │
└─────────────────────────────┘
         ↓ 点击确定
┌─────────────────────────────┐
│  填写车辆信息                │
├─────────────────────────────┤
│  车牌号：[京A88888]          │
│  风格：  [现代简约 ▼]        │
│  ...                         │
└─────────────────────────────┘
```

#### 方案B：在创建页面内嵌档案选择
```
┌─────────────────────────────┐
│  创建挪车码                  │
├─────────────────────────────┤
│  使用档案：[王先生 ▼]        │
│            138****8000       │
│                             │
│  车牌号：[京A88888]          │
│  风格：  [现代简约 ▼]        │
│  ...                         │
└─────────────────────────────┘
```

---

## 🔒 权限控制

### 验证逻辑

```java
// 1. 验证用户登录
String openid = authService.requireOpenid(authorization);

// 2. 验证档案归属
OwnerProfile profile = store.findProfile(request.ownerId())
    .orElseThrow(() -> new NotFoundException("车主档案不存在"));

if (!profile.openid().equals(openid)) {
    throw new ForbiddenException("无权使用此档案");
}

// 3. 创建卡片时使用profile的信息
VehicleCard card = new VehicleCard(
    ...,
    profile.id(),           // 关联档案ID
    profile.nickname(),     // 从档案复制昵称
    profile.phone(),        // 从档案复制电话
    ...
);
```

---

## 📝 数据库设计（未来迁移）

如果将来要迁移到真实数据库：

```sql
-- 车主档案表
CREATE TABLE owner_profiles (
    id VARCHAR(64) PRIMARY KEY,
    openid VARCHAR(128) NOT NULL,
    nickname VARCHAR(100),
    phone VARCHAR(20),
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_openid (openid)
);

-- 挪车卡片表
CREATE TABLE vehicle_cards (
    id VARCHAR(64) PRIMARY KEY,
    owner_id VARCHAR(64) NOT NULL,
    vehicle_id VARCHAR(64) NOT NULL,
    owner_name VARCHAR(100),
    plate_no VARCHAR(20),
    phone VARCHAR(20),
    comfort_message TEXT,
    card_style VARCHAR(50),
    background_color VARCHAR(20),
    theme_color VARCHAR(20),
    show_plate_badge BOOLEAN DEFAULT TRUE,
    custom_text TEXT,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (owner_id) REFERENCES owner_profiles(id) ON DELETE CASCADE,
    INDEX idx_vehicle_id (vehicle_id),
    INDEX idx_owner_id (owner_id)
);
```

---

## ✅ 优势总结

### 相比单一Owner模式的优势

1. **灵活性**
   - 一个微信用户可以管理多个身份
   - 可以为不同场景使用不同联系方式

2. **隐私保护**
   - 工作用车显示工作号码
   - 私家用車显示私人号码

3. **家庭共享**
   - 全家人共用一个微信小程序
   - 各自管理自己的车辆

4. **数据隔离**
   - 删除某个档案只影响该档案的卡片
   - 其他档案不受影响

5. **扩展性**
   - 未来可以添加更多档案属性（头像、备注等）
   - 可以为每个档案设置不同的默认风格

---

## 🚀 下一步开发建议

### 前端需要实现的页面

1. **档案管理页面** (`/pages/profiles/index`)
   - 显示所有车主档案列表
   - 添加新档案
   - 编辑档案
   - 删除档案

2. **创建卡片页面改造** (`/pages/create/index`)
   - 添加档案选择器
   - 如果没有档案，提示先创建档案

3. **车辆详情页优化** (`/pages/vehicle-detail/index`)
   - 显示该卡片使用的是哪个档案
   - 允许切换档案重新生成卡片

### 后端已完成

✅ OwnerProfile模型
✅ Repository层支持
✅ OwnerProfileController（CRUD）
✅ AuthService改为基于openid
✅ VehicleService支持ownerId参数
✅ VehicleController更新

---

## ⚠️ 注意事项

1. **向后兼容性**
   - 旧数据中的Owner已被删除
   - 需要清空内存数据重新启动

2. **数据一致性**
   - 卡片创建时复制profile信息（快照）
   - 修改profile不会自动更新已有卡片

3. **用户体验**
   - 首次使用时引导创建第一个档案
   - 提供合理的默认值

4. **安全性**
   - 严格验证档案归属
   - 防止跨用户访问

---

现在您可以重启后端测试新的架构了！🎉
